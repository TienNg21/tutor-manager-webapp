import { home as homeVi } from '@/pages/home/locale/home.vi'
import { login as loginVi } from '@/pages/login/locale/login.vi'
import { register as registerVi } from '@/pages/register/locale/register.vi'
import { account as accountVi } from '@/pages/account/locale/account.vi'

const messages = {
    vi: {
        // app: appVi;
        home: homeVi,
        login: loginVi,
        register: registerVi,
        account: accountVi
    }
}

export default messages;